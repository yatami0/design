---
type: reference
title: '部品の完成バー・台帳 — 49 部品 × 面の実測'
step: '-'
status: stable
date: 2026-08-09
updated: 2026-08-09
related: [DR-0090, DR-0094, DR-0096, DR-0097, OBS-0017, OBS-0008]
---

# 部品の完成バー・台帳

> **[部品の完成バー](部品の完成バー.md) が「定義」、本ファイルが「実測」**（[部品1](手順/部品1_完成バーを機械で閉じる.md) D13=B）。
> **時系列は [実行記録.md](実行記録.md) §部品1 が持つ。**本ファイルは**いまの状態**だけを持つ。
> 対象は 🆕 **出荷物の棚 49 部品**（工程5 で `PivotTable` を新設・story 4 本）。~~48 部品~~
> （`★ Review` 7 / `⑤ 題材` **3** / `① Tokens` 1 は出荷しないので別枠・[部品1 D4=B]）。
> 🟨 **story 数は数え方で割れる**ので**部品数で読む**（旧記述の 124 と `pnpm coverage` の 119 は母数が違う）。

## 0. 総括 —— 部品1 Q5 の答え

**「バーを既存 44 件に通す」を実行する前に、通す道具が揃っていないことが分かった。**

| 面 | バーの宣言 | B1-06 の**前** | B1-06 の**後** |
| --- | --- | --- | --- |
| **① 描画された** | 🟥 落とす | 🟥 **実装 0 行**（`() => null` の空 story が緑で通った） | 🟦 **44/44**（`preview.tsx` の `afterEach`・K6/K7 で両方向を確認） |
| **② a11y** | 🟥 critical 0 | 🟦 critical 0 | 🟦 **critical 0**（再計測） |
| **③ 状態面** | 🟨 台帳 | 🟥 44 件中 **10 件** | 🟥 **10 件**（据え置き・D6=B） |
| **④ 語彙の効果** | 🟥 落とす | 🟥 union prop 21 件中 **2 件** | 🟨 **15 件**（残り 6 件は shadcn cva 由来・D12=B） |
| **⑤ 型の閉じ** | 🟥 落とす | 🟦 `tsc` ＋ lint | 🟦 変化なし |
| **⑥ 44px** / **⑦ 意味** | 👁 人 | — | — |

★★★ 🟥 **Q5 の答え: 借金の主体は部品ではなく、バーそのものだった。**
**バーは「壊れた部品を数える道具」として作ったが、通してみたら 7 面のうち 2 面（①・④）が
文書の中にしか無く、実装が無かった。**——**部品側の借金（面③ の 34 件）より、
「測っていないのに測ったつもりでいた面」のほうが重かった。**

★ **バーの妥当性の検算**（部品1 Q5 の狙い）としては**成立している**——
**全件が一発で通ったわけでも、1 件も通らなかったわけでもない。**
🟦 面① は 44/44 通り（**バーは緩すぎない・空 story は実際に落ちる**）、
🟥 面③ は 10/44 しか無い（**バーは厳しすぎない・返せる量の借金が見えている**）。

## 1. 48 部品 × 面

**凡例**: 🟦 = 満たしている ／ 🟨 = 部分 ／ 🟥 = 無い ／ `—` = 対象外

| 棚 | 部品 | ① 描画 | ② critical | ③ 状態面 | ④ 語彙 | 語彙 prop |
| --- | --- | --- | --- | --- | --- | --- |
| ラッパー | `Button` | 🟦 | 🟦 0 | 🟥 | 🟥 | variant / size（**cva**） |
| ラッパー | `Sidebar` | 🟦 | 🟦 0 | 🟥 | 🟥 | variant / size（**cva**） |
| 自作 | `Box` | 🟦 | 🟦 0 | 🟥 | 🟦 **5 語** | `inset` |
| 自作 | `Container` | 🟦 | 🟦 0 | 🟥 | 🟦 **3 語 ＋ gutter** | `width` |
| 自作 | `Grid` | 🟦 | 🟦 0 | 🟥 | 🟦 **6 ＋ 4 語** | `columns` / `gap` / `inset` |
| 自作 | `Inline` | 🟦 | 🟦 0 | 🟥 | 🟦 **3 ＋ 4 ＋ 4 語** | `gap` / `align` / `justify` / `inset` |
| 自作 | `Section` | 🟦 | 🟦 0 | 🟥 | 🟦 **4 語 ＋ 見出しタイポ** | `gap` |
| 自作 | `Spacer` | 🟦 | 🟦 0 | — | 🟦 **3 語** | `size` |
| 自作 | `Stack` | 🟦 | 🟦 0 | 🟥 | 🟦 **4 ＋ 5 ＋ 4 語** | `gap` / `inset` / `align` |
| 自作 | `DataGrid` | 🟦 | 🟦 0 | 🟨 `empty` | 🟦 **kind ＋ emphasis** | `columns[].kind` |
| 自作 | `DescriptionList` | 🟦 | 🟦 0 | 🟥 | 🟦 **3 ＋ 2 語** | `columns` / `orientation` |
| 自作 | `StatusPill` | 🟦 | 🟦 0 | 🟥 | 🟦 **4 語（前景・面・ドット）** | `tone` |
| 自作 | `Timeline` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 自作 | `Link` | 🟦 | 🟦 0 | 🟥 | 🟦 **2 語** | `tone` |
| 自作 | `PeriodSelect` | 🟦 | 🟦 0 | 🟥 | 🟦 **3 語 ＋ 語の集合** | `width` / `value` |
| 🆕 自作 | `DatePicker` | 🟦 | 🟦 0 | 🟦 **empty / disabled ＋ 理由つき対象外** | 🟨 **3 語（`width`）／ 🟥 `mode` は測れない** | `width` / `mode` |
| 🆕 素材 | `Alert` | 🟦 | 🟦 0 | 🟨 **overflow ＋ 理由つき対象外** | 🟥 | variant（**cva**） |
| 素材 | `Avatar` | 🟦 | 🟦 0 | 🟦 States | 🟦 **3 語** | `size`（cva） |
| 素材 | `Badge` | 🟦 | 🟦 0 | 🟥 | 🟥 | variant（**cva**） |
| 素材 | `Breadcrumb` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 素材 | `Card` | 🟦 | 🟦 0 | 🟥 | 🟥 | size（**cva**） |
| 素材 | `Checkbox` | 🟦 | 🟦 0 | 🟦 States | — | なし |
| 素材 | `Dialog` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 素材 | `DropdownMenu` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 素材 | `Empty` | 🟦 | 🟦 0 | 🟥 | 🟥 | variant（**cva**） |
| 🆕 素材 | `Field` | 🟦 | 🟦 0 | 🟦 **invalid / disabled / overflow** | 🟥 | orientation（**cva**） |
| 素材 | `Input` | 🟦 | 🟦 0 | 🟦 States | — | なし |
| 素材 | `Label` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 素材 | `Pagination` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 素材 | `Popover` | 🟦 | 🟦 **0**（🆕 **開いて初めて 1 件出た**→塞いだ・[DR-0096]） | 🟥 | — | 🆕 `aria-label`（**型で必須**） |
| 素材 | `Progress` | 🟦 | 🟦 0 | 🟦 States | — | なし |
| 素材 | `RadioGroup` | 🟦 | 🟦 0 | 🟦 States / Overflow | — | なし |
| 素材 | `Select` | 🟦 | 🟦 0 | 🟥 | 🟦 **3 語 ＋ 単調性** | `width` |
| 素材 | `Separator` | 🟦 | 🟦 0 | — | — | なし |
| 素材 | `Sheet` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 素材 | `Skeleton` | 🟦 | 🟦 0 | — | — | なし |
| 素材 | `Slider` | 🟦 | 🟦 0 | 🟦 States | 🟦 **名前の到達** | なし（`label` 必須） |
| 素材 | `Switch` | 🟦 | 🟦 0 | 🟦 States | 🟦 **2 語** | `size`（cva） |
| 素材 | `Table` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 🆕 素材 | `Textarea` | 🟦 | 🟦 0 | 🟦 **States / Overflow** | — | なし |
| 素材 | `Tabs` | 🟦 | 🟦 0 | 🟥 | 🟥 | variant（**cva**） |
| 素材 | `Tooltip` | 🟦 | 🟦 0 | 🟥 | — | なし |
| Patterns | `EmptyState` | 🟦 | 🟦 0 | 🟥 | — | なし |
| Patterns | `FilterBar` | 🟦 | 🟦 0 | 🟥 | — | なし |
| Patterns | `FormLayout` | 🟦 | 🟦 0 | 🟨 `invalid` | — | なし |
| Patterns | `ListDetail` | 🟦 | 🟦 0 | 🟨 `empty` | — | なし |
| Patterns | `PageHeader` | 🟦 | 🟦 0 | 🟥 | — | なし |
| 製品・自作 | 🆕 `PivotTable` | 🟦 | 🟦 0 | 🟨 `empty` | 🟦 `intensity` | intensity（**as const 配列**） |
| Templates | `AppShell` | 🟦 | 🟦 0 | 🟨 `empty` / `overflow` | — | なし |

**集計**（🆕 2026-08-09・部品3 後）: 面① **48/48** ／ 面② critical **0/48** ／
面③ 🆕 **15/48**（10 ＋ 部品3 の 5 件＝ `DatePicker` / `Alert` / `Field` / `Textarea` ＋ 既存の再確認） ／
面④ **17/23**（語彙 prop を持つ部品のうち。🆕 **`DatePicker` の `width` は測り、`mode` は測れない**——下記）

🆕 ★★★ 🟥 **面④ は「トークンに裏打ちされた語彙」しか測れない**（部品3 Q1）——
バーの定義は「語彙 prop が**実効値**（`getComputedStyle` / `getBoundingClientRect`）で効いている」で、
**`DatePicker` の `mode`（`single` / `range`）は実効値を持たない**（振る舞いの分岐）。
**DOM の差（トリガの文言）でしか主張できない。**
★ **これは「バーが上流の部品向けに作られていた」ことの表れ**——
**shadcn の部品の語彙は cva ＝ 見た目の語彙**なので、**この形に気づく機会が無かった。**

## 2. 面③ の借金 —— 🆕 33 部品（44 → 48 で 5 件返し、4 件増えた）

🟥 **状態面を持たない 34 部品には、「対象外」の理由も書かれていない。**
バー §4 は「**『対象外』には理由を書く**」と定めているが、**書く場所が無かった**（＝ 本ファイルが場所）。

**返し方は [部品1 D6=B] に揃える**——**一括では返さず、その部品を触るときに返す。**
理由: 🟥 **34 件を機械的に埋めると「埋めたこと自体が緑に見える」**（バー D5 が C を却下した理由と同じ）。
**状態面は「絵を足す」ことではなく「この部品にどの状態があるかを決める」ことなので、部品を触るときにしか正しく決められない。**

🟨 **例外として、いま理由が確定しているものだけ `—`（対象外）にした**——
`Spacer` / `Separator` / `Skeleton`（**ポインタにもフォーカスにも反応せず、データも文字も受けない**）。

## 3. 面④ の借金 —— ~~残り 6 部品~~ 🆕 **機械の実測では 12 部品**（§5.5）

| 部品 | 語彙 prop | なぜ今回返さないか |
| --- | --- | --- |
| `Button` | variant 6 / size 7 | 🟨 **shadcn の cva 由来**（[部品1 D12=B]）——**上流が持つ契約**で、repo の語彙ではない |
| `Sidebar` | variant / size | 同上 |
| `Badge` | variant 6 | 同上 |
| `Card` | size 2 | 同上 |
| `Empty` | variant | 同上 |
| `Tabs` | variant | 同上 |

★ **線引きの根拠は「壊れ方が違う」**——🟥 [DR-0090](DR/DR-0090-token-classes-were-silently-dropped-by-tailwind-merge.md) が起きたのは **repo 語彙**のほうで、
`w-field-md` は **`src/lib/tw-merge.ts` に語を登録しないと `twMerge` が黙って落とす**。
**この失敗経路は repo 語彙に固有**（`tokens.css` と `tw-merge.ts` の 2 箇所に写しがあり、機械で守られていない）。
shadcn の cva は同じスケールの中で衝突するので `twMerge` が正しく畳む。

🟦 **ただし数えている**（除外しない）。**射程から外した瞬間に見えなくなるのがこの repo の失敗パターン**（[DR-0040](DR/DR-0040-frame-leaks-when-a-layer-is-added.md)）。

> ★★★ 🆕 🟥 **2026-08-09（部品6 C6-02）: 機械に引かせたら 6 件ではなく 12 件だった。**
> **本節の 6 件は目で数えたもの**で、**母数（「語彙 prop を持つ部品」）の正本が無かった。**
> 🟦 **機械の答えは 22 部品中 10 件が面④ 済み・借金 12 件**（→ §5.5）——
> **本節に無いのは `alert` / `avatar` / `dropdown-menu` / `field` / `sheet` / `switch` の 6 件。**
> 🟥 **語彙の書かれ方が 3 通りある**（文字列リテラル union ／ `keyof typeof` ／ `cva`+`VariantProps`）ので、
> **1 通りだけ見ると 12 件しか出ず、`Button` も `Box` も落ちる**——**目で数えると、目に入る書き方しか数えられない**
> （[DR-0099](DR/DR-0099-the-blind-spot-list-must-be-machine-derived.md) が overlay で言ったことの 2 例目）。
> 🟨 **上の「上流の cva 由来だから返さない」という線引きは変えていない**（[部品1 D12=B]）——**数だけが正確になった。**

## 4. 🟥 バーが見ていない出荷物

### 4.1 ✅ 解消したもの（部品3 C3-05）

| 部品 | 旧状態 | 🆕 実測の結果 |
| --- | --- | --- |
| `Alert` | 🟥 export しているのに story が無い | 🟦 **3 story**（Default / Variants / Overflow）。**critical 0** ／ 🟨 **serious の色の組が 1 種類増えた**（`#ff4f45 on #ffffff` ＝ `--destructive/90`・9 組目） |
| `Field` | 同上 | 🟦 **5 story**（Default / Orientation / Invalid / Disabled / Overflow）。**critical 0** |
| `Textarea` | 同上 | 🟦 **3 story**（Default / States / Overflow）。**critical 0** |
| `calendar` | 🟨 在庫のみ（export もしていない） | 🟦 **`DatePicker` 経由で出荷物に到達**（`Calendar` 単体は出さない・[部品3 D3=B]） |

🟥 **K4 の予測は外れた**——「`Field` / `Textarea` は入力系なので `label` 系の critical が出る」に賭けたが **0 件**。
★★ **ただしこれは「射程外に借金が無かった」の証明にならない**——
**story を書いたのがバーを知っている側**（D5=B で面つきに書き、`aria-label` を全部付けた）。
**K4 が測ったのは「借金の量」ではなく「書く人がバーを知っているか」だった**＝ **検体の設計の限界。**

### 4.2 🆕 🟥 新しく見つかった射程の外 —— **「開かない状態」**

**バーは story が描いた DOM しか見ない。**閉じた overlay は DOM を持たないので、
**面①・面②・面④ が全部「対象 0 件で緑」になる**（[DR-0096](DR/DR-0096-overlays-that-no-story-opens-are-invisible-to-every-check.md)）。

> 🟦 ✅ **2026-08-09（部品4）で閉じた。**一覧は**手で書かず機械が引く**ようになった
> （`node tools/opened-overlay-check.mjs`・素材層の `Primitive.Portal` から引く・[DR-0099](DR/DR-0099-the-blind-spot-list-must-be-machine-derived.md)）。
> 🟥 **手で書いていた一覧は誤っていた**——下表の取り消し線がその記録。

| 部品（`data-slot`） | 開く story | 状態 |
| --- | --- | --- |
| `dialog-content` | 🟦 **有**（`Dialog/Open`） | 🆕 **主張＋フォーカスの閉じ込めを測っている** |
| `popover-content` | 🟦 **有**（`DatePicker/Open`） | 🟥 **初回の実行で `Popover` の欠陥を落とした**（部品3） |
| `tooltip-content` | 🟦 **有**（`Tooltip/AlwaysOpen`・**手5 から**） | 🆕 **主張を足した**（🟥 ~~「開かれていない」は誤りだった~~） |
| 🆕 `dropdown-menu-content` | 🟦 🆕 **有**（`DropdownMenu/Open`） | 🟥 **`aria-hidden-focus` が出た**（→ D7=C） |
| 🆕 **`dropdown-menu-sub-content`** | 🟦 🆕 **有**（`DropdownMenu/SubMenuOpen`） | 🟥 **誰も名指ししていなかった 7 件目**（機械が引いて初めて出た） |
| 🆕 `select-content` | 🟦 🆕 **有**（`Select/Open`） | 🟥 **`aria-hidden-focus` が出た**（→ D7=C） |
| 🆕 `sheet-content` | 🟦 🆕 **有**（`Sheet/Open`） | 🟥 **「通った」のではなく axe が判定を放棄していた**（→ [DR-0098](DR/DR-0098-incomplete-was-counted-as-green.md)） |
| ~~`DropdownMenu` / `Sheet` / `Tooltip` / `Select` は 4 件とも開かれていない~~ | — | 🟥 **誤り**（`Tooltip` は開いていた・`sub-content` が漏れていた） |

★★ 🟥 **「48 部品が完成バーを通っている」の意味はここで 1 段弱まる**——
**閉じた状態しか描かない story は、その部品を 1 度も検査していない。**
🟦 **いまは機械が要求する**（静的＝主張が在るか ／ 動的＝主張が真か）。

### 4.4 🆕 ★★★ 🟥 **判定の保留（axe の incomplete）** —— 2026-08-09 に初めて数えた

**[DR-0098](DR/DR-0098-incomplete-was-counted-as-green.md)**: **落とす側も数える側も `violations` しか見ていなかった。**

| rule | 出荷物の棚 | 対象 | 扱い |
| --- | --- | --- | --- |
| `aria-hidden-focus` | **11** | `Dialog` / `DropdownMenu` / `Sheet` / `Select` の focus guard | 🟦 **`expectFocusTrapped` で我々が測る**（D7=C） |
| `color-contrast` | **10** | `Avatar` の fallback ／ `DatePicker` の日付セル・月ラベル | 🆕 🟦 **全件割れた**（下表） |
| 🟥 **`aria-valid-attr-value`** | **2** | `DropdownMenu` の `#radix-_r_4_` ／ `DatePicker` の `.border-border` | 🆕 🟦 **本物ではなかった**（下表） |
| ⬜ `bypass` | ~~102~~ | 44 story | ⬜ **harness 由来**（iframe 1 枚に story 1 つ）＝ `region` と同じ扱いにした |

★ **「対象 0 件で緑」（通算 17 例）とは別の型**——**対象は在り、検査も走り、それでも緑になる。**

#### 🆕 ★★★ 2026-08-09（部品5 D2=C）—— **保留を種類で畳んだ**（[DR-0100](DR/DR-0100-pending-judgements-split-into-three-kinds.md)）

**数える場所を作っただけでは読めない**（**23 という数字は、増えても減っても何が起きたか分からない**）。
→ **`tools/a11y-scan.mjs` に種類（`kind`）と理由（axe の `messageKey`）を持たせ、未分類は `exit 1` で落とす。**

| 種類 | 件数 | 誰の事情か | 実測 | 判断を持つ場所 |
| --- | --- | --- | --- | --- |
| **(a) `shortTextContent`**（文字が短くて本文か判定できない） | **9** | 🟥 **部品** | **`DatePicker` の日付セル 5 = `#8a8a8e on #ffffff`・比 3.43** ／ **`Avatar` の fallback 4（小・中・大・空）= `#85858b on #f2f2f7`・比 3.28**——**どちらも AA 未達で、既に violations 側に出ている色の組と同一** | [OBS-0017](OBS/OBS-0017_意味色とfillの対比が全滅している.md) |
| **(b) `bgOverlap`**（重なっていて背景色を決められない） | **1** | 🟥 **部品** | **`nav.absolute.inset-x-0.top-0`（34,70,196×28）が `.rdp-caption_label`（88,74,87×20）を覆っている。**🟦 **日付セル 42 件は 1 件も覆われていない**＝ 操作は壊れていない。**ラベル自体の比は 21.00** | 🆕 [OBS-0020](OBS/OBS-0020_カレンダーのnavが月ラベルを覆っている.md) |
| **(c) 検査器が無条件に保留** | **13** | 🟦 **axe** | `aria-hidden-focus` 11（`isModalOpen()` が `[role=dialog]` しか見ない）／ 🆕 **`aria-valid-attr-value` 2**（`aria-haspopup` があれば**参照先 ID の実在を確かめずに** `needsReview` を立てる・ソース実測。🟦 **参照先は 2 件とも実在した**） | 🟦 **畳む。引き換えに `expectFocusTrapped` / `expectOpened` で我々が測る** |

🟦 **色の組の突き合わせ**: **既知 9 件 / 新しい組 0 種類**（機械が毎回出す）。
🟦 **再現性**: 同じコミットで 2 回打って**ノード単位で完全一致**（🟥 **「再現しない」の予測は外れた**）。

### 4.3 🟨 面⑥（当たり判定 44px）—— 実測を初めて載せた

**バーは面⑥ を「👁 人」に置いている**（axe は 24px AA しか測らない・[DR-0034](DR/DR-0034-touch-target-visual-32-hit-44.md)）。
**部品3 C3-06 で `tools/hit-area-probe.mjs` を書き、`hasTouch: true` で実測した**（落とさない・D8=A）。

| 群 | 個数 | 見た目 | 当たり判定 | 44px 未達 |
| --- | --- | --- | --- | --- |
| `DatePicker` の日付セル | **42** | 28×28 | 🟥 **28×28** | 🟥 **42 / 42** |
| 月送りボタン | 2 | 28×28 | 🟥 **28×28** | 🟥 **2 / 2** |
| トリガ | 1 | 129.73×32 | 🟦 **141.73×44** | 🟦 **0 / 1** |

★★★ 🟥 **予測していなかった発見: 44px の規定には迂回路がある。**
**当たり判定の拡張は製品層の `Button`（`Action/Button.tsx` の `HIT_AREA`）にしか無い。**
`calendar.tsx` は **`buttonVariants(...)` を `<button>` に直接当てている**ので、
**見た目だけを受け取り、当たり判定を受け取らない。**
🟥 **`buttonVariants` は `src/index.ts` から export されている**＝
**使い回し先も同じ迂回ができる**（[指摘 11](共通コンポーネント思想への指摘.md)「許可リストは散文では閉じられない」と同型）。
🟨 **28px は WCAG 2.5.8（AA・24px）は満たす**ので、**axe は緑のまま**——
**「a11y が緑だから当たり判定も大丈夫」は成り立たない**ことの、これが 2 例目（1 例目は [DR-0049](DR/DR-0049-hit-area-reaches-44px-only-at-default-size.md)）。
→ **判断は [OBS-0008](OBS/OBS-0008_当たり判定44pxをどう扱うか.md) が持つ**（この回では直さない・D8=A）。

## 5. 面② の実測（🆕 2026-08-09・部品3 C3-09・`tools/a11y-scan.mjs` ＋ axe-core 4.12.1・`document` 走査）

| impact / rule | 出荷物の棚 | 除外棚 |
| --- | --- | --- |
| **critical** | 🟦 **0** | 🟦 **0** |
| 🆕 **`color-contrast` 以外の serious** | 🟥 **3**（🆕 **`aria-hidden-focus`**・`DropdownMenu` ×2 ／ `Select` ×1） | 🟦 **0** |
| serious / `color-contrast` | 🟨 **145**（story 128 題） | 195 |
| 🆕 **判定の保留（incomplete）** | 🟥 **23** → §4.4 | 🟦 **0** |
| moderate（`region` / `landmark-one-main` / `page-has-heading-one` / 🆕 `bypass`） | ⬜ **数えない**（harness 由来） | ⬜ |

> 🆕 🟥 **`aria-hidden-focus` 3 件は「落とさないが数える」**（部品4 D7=C）。
> **バーでは story 単位で rule を外し、引き換えに `expectFocusTrapped` で
> 「フォーカスが実際に閉じ込められているか」を直接測っている**——
> **外すだけなら消したのと同じ**（[部品1 D3](手順/部品1_完成バーを機械で閉じる.md) が `color-contrast` でやったのと同じ形）。
> 🟦 **根拠**: `axe-core@4.12.1` の `isModalOpen()` は **`[role=dialog]` しか見ない**ので、
> `role="menu"` / `role="listbox"` の modal overlay は原理的に判定できない。

★★ 🟥 **件数は指標として使えない**（据え置きの結論）。113 → **142**（+29）に増えたが、
**増えたのは本回の story 18 本ぶん**。🟦 **意味があるのは「色の組の種類」で、8 → 🆕 **9 種類**:

| 件数 | 色の組 | 正体 |
| --- | --- | --- |
| 83 | `#8a8a8e on #ffffff` | `--muted-foreground` on 背景 |
| 17 | `#ff9500 on #ffeed6` | warning ＋ fill |
| 12 | `#ff3b30 on #ffffff` | destructive on 背景 |
| 12 | `#828287 on #ececed` | neutral ＋ fill |
| 6 | `#34c759 on #dff6e4` | success ＋ fill |
| 6 | `#85858b on #f2f2f7` | `--muted-foreground` on `--muted` |
| 2 | `#ff3b30 on #ffebea` | destructive ＋ fill |
| 🆕 **2** | 🆕 **`#ff4f45 on #ffffff`** | 🆕 **`--destructive/90`**（`Alert` の description・**不透明度で作られた色**＝ 9 組目） |
| 2 | `#ff3b30 on #ffe0de` | destructive ＋ 濃い fill |

🆕 🟥 **9 組目は「不透明度で作った色」**——`alert.tsx` の
`*:data-[slot=alert-description]:text-destructive/90` が白地で `#ff4f45` になる。
★ **[OBS-0017](OBS/OBS-0017_意味色とfillの対比が全滅している.md) の 8 組はすべて「トークンの組」だったが、これは「トークン × 不透明度」**——
**① 層の語彙には無い色が、部品の className から生まれている**（→ [OBS-0009](OBS/OBS-0009_不透明度と状態面の概念を理解する.md) と接続する初めての実例）。

🆕 **計測器を常設化した**——`tools/a11y-scan.mjs`（`pnpm build-storybook` の後に回す）。
🟨 **pnpm では `axe-core` が hoist されない**ので、`@storybook/addon-a11y` 経由で解決している。

## 5.5 🆕 面ごとの被覆（2026-08-09・部品6 C6-02・`pnpm coverage`）

★★★ 🟥 **「バー 130/130 緑」は件数であって面積ではない**（[DR-0102](DR/DR-0102-green-must-be-read-as-an-area.md)）。

| 面 | 掛かった / 対象 | 注 |
| --- | --- | --- |
| **① 描画された** | **130 / 130** | 🟥 **どの部品が描かれたかは見ていない**（[OBS-0021](OBS/OBS-0021_面1は部品が描かれたことを見ていない.md)・closed） |
| **② a11y** | **130 / 130** | 🟥 **`color-contrast` / `region` は rule 単位で無効**（serious 148 は落ちない） |
| **③ 状態面** | 🟥 **0 / ?** | **実装が無い**（本台帳 §1 に手で載せている・借金 33 件は §2） |
| **④ 語彙の効果** | 🟥 **10 / 22（45%）** | 🆕 **母数は機械が引く**（文字列リテラル union 8 ／ `keyof typeof` 10 ／ `cva`+`VariantProps` 7） |

| 主張の量 | 全体 | 出荷物の棚 |
| --- | --- | --- |
| story | **130** | **115** |
| `play` を持つ | **27（21%）** | **27** |
| 🟥 **主張が 1 本も無い** | 🟥 **103（79%）** | 🟥 **88（77%）** |

**repo 全体の主張は 89 本**（`await expect` 77 ／ `expectOpened` 8 ／ `expectFocusTrapped` 4）。
🟥 **1 本も主張を持たない部品は 58 ファイル中 40。**

🟥 **面④ の借金 12 件**（22 − 10）: `alert` / `avatar` / `badge` / `button` / `card` / `dropdown-menu` /
`empty` / `field` / `sheet` / `sidebar` / `switch` / `tabs`。
🟨 **返し方は [部品1 D6=B] に揃える**——**一括では返さず、その部品を触るときに返す**（§2 の面③ と同じ）。
🟥 **閾値は置いていない**（部品6 D2=B）。**置く条件は「被覆が 1 度でも下がったら」**（D9）——
**総量は借金だが、下がることは新しい借金。**

### 🆕 5.5b 工程5 で母数そのものが直った（2026-08-10・工程5 D11=A / D12=A）

★★★ 🟥 **上の「10 / 22」は母数も分子も間違っていた。**新部品 `PivotTable` の `intensity` が
**母数に入らなかった**ことから追ったら、**語彙の書かれ方は 3 通りではなく 5 通り**だった。

| 経路 | 形 | 件数 | いつ足したか |
| --- | --- | --- | --- |
| literal | `prop: 'a' \| 'b'`（直書き） | 8 | 部品6 |
| 🆕 **literalAlias** | `type X = 'a' \| 'b'` に名前を付けたもの | **4** | **工程5** |
| keyof | `type X = keyof typeof CONST` | 10 | 部品6 |
| 🆕 **constUnion** | `type X = (typeof CONST)[number]` | **3** | **工程5** |
| cva | `cva(...)` ＋ `VariantProps` | 7 | 部品6 |

🟥 **落ちていたのは新部品だけではない**——`PERIOD_PRESETS`（工程3）／ `DATE_PICKER_MODES`（部品3）／
`DataGridColumnKind`（手8d）／ `StatusTone`（工程2）**も 1 度も母数に入っていなかった。**

★ **分子も直した**——判定が `measure.ts` の 4 関数の呼び出しでしか立たず、
**生の `getComputedStyle` で書いた主張を 0 と数えていた**（`DataGrid` / `DescriptionList` / `Link` / `StatusPill` / `PivotTable`）。

| 面④ | 部品6（2026-08-09） | 🆕 工程5（2026-08-10） |
| --- | --- | --- |
| 掛かった / 対象 | 10 / 22（45%） | 🟦 **15 / 27（56%）** |

🟥 **上がったのは実装が良くなったからではない。測り方が正確になったから。**
🟨 **面④ の借金は 12 → 12 件**（`alert` / `avatar` / `badge` / `button` / `card` / `dropdown-menu` /
`empty` / `field` / `sheet` / `sidebar` / `switch` / `tabs`）——**顔ぶれは変わっていない。**

🟥 **主張 0 本の story は 103/130（79%）→ 105/137（77%）。**工程5 は **7 story 中 5 本に主張を書いた。**

## 5.6 🆕 測る側の網（2026-08-09・部品6 C6-04・[DR-0103](DR/DR-0103-the-measuring-side-had-no-net.md)）

| 網 | コマンド | 現況 |
| --- | --- | --- |
| **型** | `pnpm typecheck:tools` | 🟥 **56 件**（`a11y-scan` 25 / `edit-probe` 17 / `visual-probe` 6 / `hit-area-probe` 4 / `opened-overlay-check` 3 / `title-map-check` 1 / `dead-class-scan` 0 / 🟦 **本回の新規 2 本は 0**） |
| **赤テスト** | `pnpm self-check` | 🟦 **8 / 8**（`opened-overlay-check` 4 本 ／ `coverage-scan` 4 本） |

🟥 **`self-check` が覆っているのは 2 本だけ**——**`a11y-scan` を含む 6 本にはまだ検体が無い**
（`a11y-scan` は Storybook のビルドを要求するので、検体の作り方が別問題になる）。

## 6. 更新の作法

- **部品を足したら本ファイルに 1 行足す。**足さないと [DR-0091] の「story が無い部品は数えられない」を繰り返す
- **面③ の「対象外」は、その部品を触ったときに理由つきで埋める**（D6=B）
- 🟥 **面② の件数は毎回 story 数と一緒に書く**（件数だけ書くと増減の意味が読めない）
