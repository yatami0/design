---
id: DR-00XX
type: decision # decision（決めたこと） | finding（分かったこと）
title: '{一行で。決定なら体言止め、発見なら主張の形で書く}'
status: decided # decided | observed | superseded
date: 2026-MM-DD
step: 手N # 手0〜手9 / - （手に属さない場合）
related: [] # 他の DR の id
poc_feedback: null # PoC へ戻す候補なら行き先を書く（例: 'OBS 候補' / 'ADR-0019 の材料'）。無ければ null
---

# DR-00XX: {タイトル}

## 背景

{なぜこれを決める／調べることになったか。1〜3 行}

## {決定 | 発見}

{決めたこと、または分かったことを箇条書きで。**推測と実測を混ぜない**}

## 根拠（実測）

{何をどう確かめたか。コマンド・出力・ファイルの行番号。**「一般にこうだから」は根拠にならない**}

## 影響

**観測から直接言えること**

{根拠に書いた実測だけで支えられること。**ここに「はず」を書かない**}

**🟥 推論（未検証）**

{そこから考えたこと。**後続の手はこれを前提に数を数えない**。検証する方法が分かるなら添える}

## 関連

- 手順書: {docs/手順/...}
- 実測の記録: {docs/実行記録.md §手N}

<!-- ============================================================
     書き方の規律
     - 1 ファイル = 1 決定 or 1 発見。混ぜない
     - 決定は不変に積む。覆すときは本文を書き換えず、新しい DR で supersede し
       この DR の status を superseded にして related に追記する
     - 発見（finding）は「何が観測されたか」に徹する。対処は決定（decision）側で書く
     - §影響 は「観測から直接言えること」と「推論」に分ける（DR-0055）。
       節名が推論を誘うので、書く前に「これは見たことか、考えたことか」を一度問う。
       混ぜると後続の手が推論を事実として読み、それを前提に数を数える（OBS-0007・3 例）
     ============================================================ -->
