# ロックファイル・ビルド出力
pnpm-lock.yaml
dist/
node_modules/
# markdown は対象外（PoC の DR-0012 と同じ理由: 表の再整形で全行 diff になる）
**/*.md

# shadcn が置いたコード（手1 の D11）
# 整形すると「shadcn が吐いたそのもの」でなくなり、手5 の「部品を触らずに」の証拠性が落ちる。
# また shadcn add / update のたびに全行 diff になる。
# ⚠ 編集禁止という意味ではない（PoC の src/generated/ とは性質が違う）。整形対象外というだけ。
# 自分で書く部品（Layout プリミティブ等）は src/components/ 直下に置き、整形対象に残す。
src/components/ui/
src/hooks/
src/lib/utils.ts

# 手6: /design-sync の生成物・staging（eslint.config.mjs の ignores と同じ集合）
.design-sync/sb-reference/
.design-sync/.cache/
ds-bundle/
.ds-sync/

# 工程2: MSW の worker スクリプト（`msw init` の生成物・361 行）
# ⚠ ファイル冒頭に「Please do NOT modify this file」と書かれている vendor コード。
# 整形すると msw の版と結びついた原本でなくなる（src/components/ui/ と同じ扱い）。
# 🟥 除外する前に赤の件数を測った（prettier 1 件・cspell 4 件・eslint warning 1 件）——
#    DR-0040（層を足すたびに射程が漏れる）の再演。内訳は 実行記録 §工程2。
public/mockServiceWorker.js

# 手7: Claude Design が生成した成果物のスナップショット（証拠）
# 🟥 整形すると「design agent が吐いたそのもの」でなくなり、Q1〜Q3 の数え上げの証拠性が落ちる。
# ds-bundle/ と同じ扱い。DR-0040（層を足すたびに射程が漏れる）の 4 例目。
artifacts/

# 部品1（2026-08-09）: Conductor の作業領域（agent 間の受け渡し用スクラッチ）。
# 🟥 repo の .gitignore ではなく Conductor が .git/info/exclude 側で除外しているので、
#    git からは見えないのに prettier からは見える——**除外の口が 2 系統ある**形。
#    DR-0040（層を足すたびに射程が漏れる）の 5 例目。実測: a11y 走査の生データ 2 件で format:check が赤くなった。
.context/
